# data-sourcing-and-apis
## scratch
* Auth & secrets: .env, os.getenv, fail-fast checks, headers vs params

* Requests basics: GET/POST, timeouts

* URL building: base+path, params=, f-strings, reusable parameterized calls

* Responses: status codes, raise_for_status, response.json, json.dumps (debug)

* Pagination & rate limits: page/offset/cursor loops, stop conditions, sleep

* Robustness: try/except, retries/backoff, basic logging

* JSON → tabular: nested traversal, pd.json_normalize, schema/key checks, null handling

* Data hygiene: dtypes, renaming, dedupe, raw vs cleaned outputs, arrays/explodes

* SDK vs raw HTTP: know what SDKs abstract; show raw proficiency